controller:
  kind: DaemonSet
  
  reportNodeInternalIp: true

  hostPort:
    enabled: false

  service:
    type: LoadBalancer  # Change to LoadBalancer
    ports:
      http: 8080
      https: 8443