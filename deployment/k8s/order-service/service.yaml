apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: s-shop-system
  labels:
    app: order-service
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8000"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: web
      port: 8000
      targetPort: web
    - name: grpc
      port: 9000
      targetPort: 9000
