kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: ingress
  namespace: s-shop-system
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: "arch.homework"
      http:
        paths:
          - path: /
            backend:
              service:
                name: api
                port:
                  number: 8000
            pathType: Prefix